import React from "react";
import Tooltip from "@material-ui/core/Tooltip";

export default function SimpleTooltips() {
  return (
    <div>
      <Tooltip title="Save Changes" aria-label="Save Changes">
          {/* Button or icon */}
      </Tooltip>
    </div>
  );
}
